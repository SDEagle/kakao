<%= link_to 'Kakao', root_path, :class => 'navbar-brand' %>
<ul class="nav navbar-nav pull-right">
  <% if user_signed_in? %>
      <li>
        <%= link_to current_user.name, current_user %>
      </li>
      <li>
        <%= link_to(t('devise.basic.edit'), edit_user_registration_path) %>
      </li>
      <li>
        <%= link_to(t('devise.basic.sign_out'), destroy_user_session_path, :method => 'delete') %>
      </li>
<% else %>
      <li>
      <%= link_to(t('devise.basic.sign_in'), new_user_session_path) %>
      </li>
      <li>
      <%= link_to(t('devise.basic.sign_up'), new_user_registration_path) %>
      </li>
  <% end %>
</ul>